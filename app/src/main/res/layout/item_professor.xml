<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginTop="5dp"
    android:layout_marginHorizontal="10dp"
    android:layout_marginBottom="5dp"
    app:cardCornerRadius="4dp"
    app:cardElevation="5dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp">
        <ImageView
            android:id="@+id/imgProfessor"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:padding="4dp"
            android:src="@drawable/ic_add_professor"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <TextView
            android:id="@+id/tvProfessorName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="18sp"
            tools:text="Julio Cesar Camacho Silva"
            android:layout_marginStart="10dp"
            app:layout_constraintTop_toTopOf="@+id/imgProfessor"
            app:layout_constraintStart_toEndOf="@+id/imgProfessor"
            app:layout_constraintEnd_toEndOf="parent" />
        <TextView
            android:id="@+id/tvProfessoEmail"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            tools:text="Julio Cesar Camacho Silva"
            app:layout_constraintTop_toBottomOf="@+id/tvProfessorName"
            app:layout_constraintStart_toStartOf="@+id/tvProfessorName"
            app:layout_constraintEnd_toEndOf="@+id/tvProfessorName"
            app:layout_constraintBottom_toBottomOf="@+id/imgProfessor"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>